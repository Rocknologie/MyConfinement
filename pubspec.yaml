name: myconfinement
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera:
  path_provider:
  image_picker:
  path:
  cupertino_icons: ^0.1.2
  compasstools: ^0.1.0
  cloud_firestore:
  google_maps_flutter:
  location: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/
